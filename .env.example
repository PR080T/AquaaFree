# Core Database Configuration
#----------------------------
# MongoDB Atlas connection string (required both locally and on Vercel)
# Replace <db_password> with your actual password
DATABASE_URL="mongodb+srv://prasanna:prasanna@aquaafree.jtpivhi.mongodb.net/AquaaFreeDB?retryWrites=true&w=majority&appName=AquaaFree"

# Optional Redis Configuration (used for distributed rate limiting)
#-----------------------------------------------------------------
# Leave unset to fall back to in-memory rate limiter
# REDIS_URL="redis://localhost:6379"

# Optional Email Configuration (SendGrid)
#---------------------------------------
# Leave these blank if email sending should be skipped
# SENDGRID_API_KEY=""
# FROM_EMAIL=""
# TO_EMAIL=""

# Optional Security / Runtime Settings
#------------------------------------
# NODE_ENV="development"

# Reference Commands
#-------------------
# npm install
# npx prisma generate
# npm run dev
