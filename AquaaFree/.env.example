# Database Configuration
#-----------------------
# PostgreSQL URL format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL="postgresql://user:password@localhost:5432/aquaafree"
# For development, you can use SQLite:
# DATABASE_URL="file:./dev.db"

# Redis Configuration (Optional but recommended for production)
#-----------------------
# Format: redis://[[username][:password]@][host][:port][/db-number]
REDIS_URL="redis://localhost:6379"
# For Redis with password:
# REDIS_URL="redis://default:your_password@localhost:6379"

# SendGrid Email Configuration
#-----------------------
# Required for email notifications
SENDGRID_API_KEY="your_sendgrid_api_key"
# Email used as sender for notifications
FROM_EMAIL="notifications@aquaafree.example"
# Admin email to receive notifications
TO_EMAIL="admin@aquaafree.example"

# Rate Limiting Configuration
#-----------------------
# Time window in milliseconds (default: 1 minute)
RATE_LIMIT_WINDOW=60000
# Maximum requests per window
RATE_LIMIT_MAX_REQUESTS=10

# Security Configuration
#-----------------------
# CORS Origins (comma-separated list)
ALLOWED_ORIGINS="http://localhost:3000"
# JWT Secret for admin authentication (min 32 chars)
JWT_SECRET="your-super-secret-jwt-key-min-32-chars"

# Development Settings
#-----------------------
# Set to "development" or "production"
NODE_ENV="development"
# Enable/disable detailed error messages
DEBUG="false"

# After setting up environment:
# 1. Install dependencies:
#    npm install
#
# 2. Initialize Prisma:
#    npx prisma migrate dev --name init
#    npx prisma generate
#
# 3. Start development server:
#    npm run dev
